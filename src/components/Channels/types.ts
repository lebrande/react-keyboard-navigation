export type Channel = {
  name: string;
  id: string;
  image: string;
};

export type ChannelVideo = {
  id: string;
  channelTitle: string;
  description: string;
  imageUrl: string;
  publishedAt: string;
  title: string;
};
