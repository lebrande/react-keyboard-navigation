export type Channel = {
  name: string;
  channelId: string;
  image: string;
};

export type ChannelVideo = {
  channelTitle: string;
  description: string;
  imageUrl: string;
  publishedAt: string;
  title: string;
};
